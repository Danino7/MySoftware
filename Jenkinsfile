pipeline {
    agent any

    stages {
        stage('click') {
            steps {
                sh 'dir'
                sh 'click.py'
                sh 'welcome.py'
            }
        }
    }
}
